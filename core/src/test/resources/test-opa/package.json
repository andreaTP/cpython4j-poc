{
    "scripts": {
        "build": "esbuild index.mjs --bundle --outfile=dist/out.js --format=esm --platform=node",
        "run": "node dist/out.js",
        "clean": "rimraf dist node_modules"
    },
    "devDependencies": {
        "esbuild": "0.25.2",
        "rimraf": "^6.0.1"
    },
    "dependencies": {
        "@open-policy-agent/opa-wasm": "^1.10.0",
        "yaml": "^2.7.1"
    }
}
